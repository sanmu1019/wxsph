services:
  wxsph-api:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/app/data
    environment:
      WXSPH_CACHE_FILE: /app/data/wxsph_cache.json
